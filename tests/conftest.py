"""Shared pytest fixtures for v1-routines tests.

Phase R6 fills in real fixtures (mocked Routine HTTP triggers via respx,
fake GitHub raw URL responses, a TestClient for dashboard.py). This
skeleton just establishes the file so import-time collection works once
tests start landing.
"""
